import React from 'react'

const Defaultlayout = ({children}) => {
    return (
        <div>
            bu default layout
            {children}
        </div>
    )
}

export default Defaultlayout
